{
  "schemaVersion": 1,
  "currentStage": "5a",
  "currentStageTitle": "Stage 5 — tune the [TUNABLE] params to the §9 targets",
  "status": "not-started",
  "nextAction": "Stage 5a (STAGE 4 is COMPLETE — the harness exists & runs: `npm run sim` competitive, `npm run sim -- --bloodpact` traitor). Tune src/v2/tunables.ts to hit the §9 targets using the sim as the measuring instrument. PRIORITIZED by the baseline sweep (sim-results/sample/): (1) THE GAMBIT IS OVERPOWERED — fires 49% (target 10-20%), gambler wins 50.2% vs 26.4% even-share → raise GAMBIT_SURCHARGE / make seize harder / re-examine the gambit win timing. (2) Shadowking win 15.3% → 18-22% (strengthen the dark slightly). (3) rescues 0.2/game → 2-4 (the rescue economy barely fires — RESCUE_COST / broken dynamics / AI rescueWillingness). (4) Blood Pact: accusation accuracy 20.3% (AIs over-accuse) — tune ACCUSATION_* knobs and/or the chooseAccusation threshold so accusations catch traitors more than random. RE-RUN the sweep after each change; prove no archetype dominates and free-riding stays unrewarded. Consider the Stage-5 tunable-injectability seam (plan-stage4.md §4 Option 1) so the sim can sweep params automatically.",
  "specRefs": [
    "ALGORITHM §9 (the [TUNABLE] list + target metrics)",
    "sim-results/sample/REPORT.md + BLOOD_PACT.md (the baseline findings to fix)",
    "src/v2/tunables.ts (the knobs), src/v2/sim/* + `npm run sim` (the instrument)",
    "ROADMAP §4 (5a)"
  ],
  "lastVerified": {
    "timestamp": "2026-06-22T19:35:25.753Z",
    "commit": "440fb53",
    "sourceHash": "5994297d2ad69b22a466100bd45daeab3228cf4c25206256d2f88d5370257934",
    "typecheck": "pass",
    "lint": "pass",
    "tests": {
      "files": 20,
      "passed": 352,
      "failed": 0,
      "status": "pass"
    }
  },
  "dirty": true,
  "invariants": [
    "All mutation goes through applyCommand (src/v2/reducer.ts) — no direct state mutation elsewhere",
    "No Math.random / Date.now / any in src/v2 — all RNG via SeededRandom (ALGORITHM §7)",
    "GameState is JSON-serializable (clone = JSON.parse(JSON.stringify))",
    "AI decisions must be pure f(state, seed) (ALGORITHM §7.9) — required for reproducibility",
    "Layer B (blood_pact) only: pledges emit PLEDGE_COMMITTED (concealed) not PLEDGE_SUBMITTED; real tiers go to suspicionLog; the traitor forfeits the doom_complete win iff bloodPactExposed. Competitive mode behavior is unchanged."
  ],
  "gotchas": [
    "`npm test` includes ~50 RED v1 tests by design (they anchor v1) — use `npm run test:v2` for the v2 suite",
    "vitest output buffers when piped through `tail` — redirect to a file, or use the JSON reporter (verify.mjs does)",
    "Run `npm run verify` to (re)populate this block; never hand-edit lastVerified/dirty",
    "ACTION pointer quirk: after the LAST player in turnOrder exhausts actions, advanceActivePlayer leaves activePlayerIndex ON that player (turnOrderPosition === turnOrder.length). Any per-player action loop must ALSO stop on actionsRemaining===0, not just the index — else it re-enters a 0-action player (runAITurn handles this).",
    "The v2 UI lives in src/ui-v2 (entry index.html → src/ui-v2/main.ts; old v1 harness preserved at index-v1.html). It is OUTSIDE the verify lint/test scope (src/v2 tests/v2), but tsc --noEmit covers all of src/ so it must typecheck. Validate UI changes with `npm run dev` (or `npx vite build`); the headless session driver has logic coverage in tests/v2/ui-session.test.ts."
  ],
  "openRisks": [
    "STAGE-4 SWEEP FINDINGS (3240 games, untuned defaults — sim-results/sample/REPORT.md): (a) the Crown's Gambit is OVERPOWERED — fires 49% of games (target 10-20%) and the 'gambler' archetype wins 50.2% vs 26.4% even-share → the #1 Stage-5 tuning target (raise GAMBIT_SURCHARGE / make seize harder). (b) Shadowking win 15.3% (target 18-22%, slightly weak). (c) rescues 0.2/game (target 2-4, far too rare — rescue economy needs attention). (d) aggressor wins only 6.2% (raiding currently weak). GOOD: free-riding is NOT rewarded (winners pledge 1.60 vs field 1.79) — the 3f anti-free-rider reward works.",
    "The anti-free-rider reward (§4.2 step 5, Stage 3f) now MEASURES as working in the Stage-4 sweep (winners don't pledge less) — but PLEDGE_SHIELD_AMOUNT / PLEDGE_FAVOR_GRUDGE_REDUCTION are still placeholder [TUNABLE]; confirm it holds after Stage-5 retuning.",
    "Blood Pact accusation tuning (AUDIT_COST, ACCUSATION_* penalties/cooldown, SUSPICION_LOG_ROUNDS, TRAITOR_EXPOSED_WOUNDS) is placeholder — must be balanced in Stage 5 so accusation is neither spammed nor never-fired.",
    "Stage 3f effect-table / Last-Stand / rescue-debt magnitudes (SURGE_SPREAD_MULT, DK_MARCH_DISTANCE, GAMBIT_ADJACENT_STRIKE_MULT, RESCUE_DEBT_MIN_PLEDGE, the deterministic Last-Stand 'fight if you can win' rule) are reasonable defaults, not balanced — Stage-5 sim must validate them.",
    "Archetype AI (Stage 4b) now pursues RAID/RESCUE/Gambit; Last Stand remains engine-auto and Audit/Accusation are NOT yet in the AI action loop (wired in 4f for Blood Pact) — the Stage-4 competitive sweep under-exercises the BP deduction paths until then."
  ]
}
